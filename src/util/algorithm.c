#include "algorithm.h"

short smin(short a, short b) {
  return a < b ? a : b;
}

int min(int a, int b) {
  return a < b ? a : b;
}

short smax(short a, short b) {
  return a > b ? a : b;
}

int max(int a, int b) {
  return a > b ? a : b;
}
